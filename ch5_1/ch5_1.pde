// Exercise 5-1: Consider a grading system where numbers 
// are turned into letters. Fill in the blanks in the 
// following code to complete the boolean expression.

float grade = random(0,100);
if (/* to do*/) {
  println("Assign letter grade A.");
} else if (/* to do*/) { // In one conditional statement, you can only ever have one if and one else. However, you can have as many else if's as you like!
  println (/* to do*/);
} else if (/* to do*/) {
  println (/* to do*/);
} else if (/* to do*/) {
  println (/* to do*/);
} else {
  println (/* to do*/);
}